import 'package:flutter/material.dart';

class EquationHistory extends StatelessWidget {
  const EquationHistory({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('3'),
    );
  }
}
